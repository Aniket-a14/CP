#include<bits/stdc++.h>
using namespace std;

class Solution{
    public:
        string lcs(string& s, string& t){
            vector<int> dp(s.size(),vector<int>(t.size()+1,-1))
        }
};  

int main(){
    string s,t;
    cin>>s>>t;

    Solution s;
    cout<<s.lcs(s,t)<<endl;
}